#
# Core dependencies for understatapi
# These are also defined in pyproject.toml [project.dependencies]
# With modern pyproject.toml, this file is optional but kept for backwards compatibility
#
requests>=2.32.0
urllib3>=2.6.0
certifi>=2024.0.0
charset-normalizer>=3.0.0
idna>=3.6
