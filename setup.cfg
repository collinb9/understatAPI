# This file is kept for backwards compatibility
# All configuration has been moved to pyproject.toml
# You can safely delete this file if you don't need setuptools-specific options

[options.packages.find]
exclude =
    test*
    docs*
